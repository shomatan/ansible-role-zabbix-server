galaxy_info:
  author: Shoma Nishitateno
  description: Installs Zabbix server
  license: "license BSD"
  min_ansible_version: 1.8
  platforms:
    - name: EL
      versions:
        - all
  galaxy_tags: []

dependencies: 
  - src: shomatan.php-fpm
    when: "ansible_connection != 'docker'"
  - src: shomatan.mariadb
    when: "ansible_connection != 'docker'"